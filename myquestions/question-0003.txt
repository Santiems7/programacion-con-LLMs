En una estación meteorológica se registran mediciones ambientales para predecir si habrá lluvia al día siguiente.
Cada fila del DataFrame representa un día de observación, y las columnas contienen mediciones numéricas como: temperatura máxima,  humedad, velocidad del viento, presión atmosférica. 
Sin embargo, algunos sensores fallan ocasionalmente, así que algunas celdas tienen valores faltantes (NaN). Además, como las variables están en distintas escalas, deben normalizarse antes de usarlas en un modelo de machine learning.
Escribe una función llamada procesar_datos_meteorologicos(df, target_col) que realice lo siguiente:
1.	Separe las características X de la variable objetivo y, indicada por target_col. 
2.	Impute los valores faltantes en X usando el promedio de cada columna, utilizando SimpleImputer de sklearn. 
3.	Escale las características imputadas para que tengan media 0 y desviación estándar 1, utilizando StandardScaler de sklearn. 
4.	Devuelva dos arrays de numpy: 
    o	la matriz X procesada, 
    o	el vector y.
